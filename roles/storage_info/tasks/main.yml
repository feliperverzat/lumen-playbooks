---
  - name: collect all information
    purefa_info:
        gather_subset:
          - "{{ subset }}"
        fa_url: 10.220.14.5
        api_token: 2475f960-2a79-21d0-5994-ef374bde6689
    register: array_info
  - name: show all information
    debug:
      msg: "{{ array_info['purefa_info'] }}"
  
  - set_fact:
      array_info_list: "{{ array_info_list + [  item | json_query(query) ] }}"
    with_items: "{{ array_info.results }}"
    vars:
      query: '"hosts"."wwn"'
  - name: show all information
    debug:
      msg: "{{ array_info_list }}"

...
